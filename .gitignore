# Superpowers brainstorming mockups — local only, never ship to Pages
.superpowers/

# Superseded placeholder roster from early development, kept only as a
# local reference. Publishing it would sit next to the real data.json
# looking like a current backup.
data_backup.json

# Design reference dropped into screenshots/ — the standings mockup the team
# page was built from, not a match screenshot. build-screenshots.mjs skips it
# (the name does not parse) and it should not ship with the real ones.
screenshots/image.png

# OS / editor noise
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/

# Node (only present if dev tooling is added later)
node_modules/

# Browser-automation dumps (page snapshots, console logs) from checking the
# live site. Debug output, and the repo is public.
.playwright-mcp/
